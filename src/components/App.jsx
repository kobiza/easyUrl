import React, { Component, PropTypes } from 'react';
import EditCompPanel from './EditCompPanel.jsx';

export default class App extends Component {
  render() {
    return (
      <div className="app">
        <EditCompPanel/>
      </div>
    );
  }
}
